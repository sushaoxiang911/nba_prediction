FROM n8nio/n8n:latest

# For Cloud Run, we don't need to expose anything specific,
# but we'll respect the PORT env var (n8n does this already as of recent versions).
# Entrypoint is already set to n8n in the official image.
